import 'package:flutter/material.dart';

class UsersChatsView extends StatelessWidget {
  const UsersChatsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("UsersChatsView"));
  }
}
